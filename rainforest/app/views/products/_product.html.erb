

  <div class="col-sm-6 col-md-4">
    <div class="thumbnail">
      <img src="<%= product.url %>" alt="..."> 
      <div class="caption">
        <h3><%= product.name %></h3>
        <p><%= product.description %></p>
        <%= link_to "View More", product_path(product), :class => 'btn btn-primary', :role => 'button' %>
      </div>
    </div>
  </div>
